{
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/auth/login/"
        
      },
      {
        "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }]
      }
    ]
}